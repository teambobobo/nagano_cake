<div class='container'>
  <div class='row'>
    <div class="col-md-8 px-5 mx-auto">
<!--商品詳細画面-->
<!--ここでのafter_taxはItemモデルに定義したメソッドのこと-->
      <h2>商品詳細</h2>
      <div>
        <%= image_tag @item.image %>
        <p>商品名：<%= @item.name %></p>
        <p>商品説明：<%= @item.introduction %></p>
        <p>ジャンル：<%= @item.genre.name %></p>
        <p>税抜価格(税込価格) ：<%= @item.before_tax.to_s(:delimited) %>(<%= @item.after_tax.to_s(:delimited) %>)円</p>
        <p>販売ステータス：<%= @item.sale_status(@item) %></p>
      </div>

      <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-success" %>

    </div>
  </div>
</div>